{
  "name": "embrayse-marketing",
  "version": "0.1.0",
  "private": true,
  "description": "Embrayse marketing site. Main pages are vanilla HTML/CSS/JS; /insights is built with Eleventy.",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "clean": "rm -rf insights"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-rss": "^2.0.2"
  }
}
